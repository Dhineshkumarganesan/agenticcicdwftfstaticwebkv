Step1
Update cicd/contract.yml — add a resources section declaring 
 an Azure Storage Account for static website hosting, LRS replication, 
 all 3 environments (dev, test, prod), region westeurope.

Step 2

 @terraform-module-expert Read cicd/contract.yml and scaffold 
 the declared resources into infra/envs/dev/main.tf, 
 infra/envs/test/main.tf, and infra/envs/prod/main.tf. 
 Follow the locals/tags pattern already in each file. 
 Add outputs to each outputs.tf.

Step 3.
@terraform-module-expert
 CI has passed for the Storage Account infrastructure in all 3 environments.
 
 Please add standard resource tags to all resources in infra/envs/dev, 
 infra/envs/test, and infra/envs/prod:
 
   tags = {
     environment = var.environment
     project     = "agenticcicdwftfstaticwebkv"
     managed_by  = "terraform"
   }
 
 Add an `environment` variable to each variables.tf if not already present.
 Commit and push directly to main to trigger the CD pipeline.






